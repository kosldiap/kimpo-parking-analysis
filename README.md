# 김포공항 주차장 주차관제시스템 데이터 분석

김포공항 주차장(국내선1·국내선2·국제선·화물청사·국제선버스·항공지원센터)의
2016~2018년 3개년 입·출차 raw data를 분석하는 코드 저장소입니다.

> ⚠️ **원본 데이터는 이 저장소에 포함되지 않습니다.** raw data(.xls)는 대용량(약 2.5GB)이며
> 차량번호 등 개인정보를 포함하므로 `.gitignore`로 제외됩니다. 코드만 버전 관리합니다.

## 데이터 위치 (로컬)

```
G:\조재원 백업(내부 정리중)\D드라이브\김포공항 주차빌딩\주차관제시스템 데이터\교통센터 관련 (3)
```

분석 스크립트는 이 경로를 `DATA_DIR`로 참조합니다. **다른 PC(예: 집)에서는 코드를 수정하지 말고** 아래 둘 중 하나로 경로만 지정하세요 (config.py가 자동 인식):

1. **환경변수** (권장) — PowerShell:
   ```powershell
   [Environment]::SetEnvironmentVariable("KIMPO_DATA_DIR", "D:\내데이터\교통센터 관련 (3)", "User")
   ```
   (새 터미널부터 적용)
2. **로컬 파일** — `src/data_dir.local.txt` 에 데이터 폴더 경로 한 줄 작성 (이 파일은 git에 올라가지 않음)

이렇게 하면 config.py는 회사·집에서 **동일**하게 두고 경로만 PC별로 달라져 git 충돌이 없습니다.

### 폴더 구조
- **2016년**: `16년도_전체주차장_N월.xls` — 월별 전체 주차장 통합 (12개)
- **2017·2018년**: 주차장별 분리
  - 국내선1 / 국내선2 / 국제선 / 화물청사: 월별 (각 12개)
  - 국제선버스 / 항공지원센터: 반기별 (1~6월, 7~12월)

## ⚠️ 데이터 사용 시 필수 주의사항 (원본 `참고사항.hwp` 기준)

1. **주차권번호 `9999999` 행은 통계 작성 시 반드시 제외** — 이미 출차완료된 차량을 다시 출차시킨 허수 내역.
2. **2016년 1~4월은 DB서버 장애로 데이터 누락** — 해당 기간 집계는 불완전.
3. **국제선버스**: 2016-02-06 운영 시작 (이전 데이터 없음 = 정상).
4. **항공지원센터**: 2016-07-20 운영 시작 (이전 데이터 없음 = 정상).

## 구조

```
kimpo-parking-analysis/
├── src/          # 분석 스크립트 (.py)
├── notebooks/    # 탐색용 주피터 노트북
├── output/       # 산출물 (gitignore 제외)
└── README.md
```

## 환경

- Python 3.x
- 주요 라이브러리: pandas, openpyxl/xlrd, olefile (hwp 파싱)
